
name: 'dbt_gestao_a_vista'
version: '1.0.0'

profile: 'dbt_gestao_a_vista'


model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"


models:
  dbt_gestao_a_vista:
    stg_gestao_a_vista:
      +materialized: view
      +schema: stg_gestao_a_vista

    int_gestao_a_vista:
      +materialized: view
      +schema: int_gestao_a_vista

    mrt_gestao_a_vista:
      +materialized: table
      +schema: mrt_gestao_a_vista
